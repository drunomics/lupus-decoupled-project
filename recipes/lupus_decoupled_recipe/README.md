# Lupus Decoupled Drupal Project Recipe

A Drupal recipe to setup the base project. This extends our Lupus [Decoupled Drupal Recipe](https://www.drupal.org/project/lupus_decoupled_recipe/) to additionally install the
`services_env_parameter` module.
